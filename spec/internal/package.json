{
  "name": "app",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.25.9"
  },
  "scripts": {
    "build:js": "node esbuild.config.js",
    "build": "npm run build:js && bundle exec rake active_admin:build",
    "watch:js": "node esbuild.config.js --watch",
    "watch:css": "bundle exec rake active_admin:watch",
    "watch": "npm run watch:js"
  },
  "dependencies": {
    "@activeadmin/activeadmin": "^4.0.0-beta19",
    "@rails/ujs": "^7.1.400",
    "activeadmin-tom_select": "file:../../npm-package",
    "tom-select": "^2.4.3"
  }
}
